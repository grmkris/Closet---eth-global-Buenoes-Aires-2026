{
  "name": "@ai-stilist/redis",
  "type": "module",
  "exports": {
    ".": "./src/redis.ts"
  },
  "scripts": {
    "build": "tsdown",
    "lint": "ultracite check",
    "fix": "ultracite fix",
    "fix:unsafe": "ultracite fix --unsafe",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "tsdown": "catalog:"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "ioredis": "catalog:",
    "@ai-stilist/logger": "workspace:*",
    "@ai-stilist/shared": "workspace:*"
  }
}
