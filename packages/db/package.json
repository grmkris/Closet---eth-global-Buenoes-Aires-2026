{
  "name": "@ai-stilist/db",
  "type": "module",
  "exports": {
    ".": "./src/db.ts",
    "./schema/auth": "./src/schema/auth/index.ts",
    "./schema/auth/index": "./src/schema/auth/index.ts",
    "./schema/wardrobe": "./src/schema/wardrobe/index.ts",
    "./schema/wardrobe/index": "./src/schema/wardrobe/index.ts"
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "lint": "ultracite check",
    "fix": "ultracite fix",
    "fix:unsafe": "ultracite fix --unsafe",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:start": "docker compose up -d",
    "db:watch": "docker compose up",
    "db:stop": "docker compose stop",
    "db:down": "docker compose down"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.14",
    "drizzle-kit": "^0.31.7",
    "tsdown": "catalog:"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@ai-stilist/logger": "workspace:*",
    "@ai-stilist/shared": "workspace:*",
    "drizzle-orm": "catalog:",
    "drizzle-zod": "catalog:",
    "zod": "catalog:"
  }
}
