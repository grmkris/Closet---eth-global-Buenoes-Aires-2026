{
  "name": "@ai-stilist/auth",
  "exports": {
    ".": {
      "types": "./dist/auth-config.d.ts",
      "default": "./src/auth-config.ts"
    },
    "./*": {
      "types": "./dist/*.d.ts",
      "default": "./src/*.ts"
    }
  },
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "lint": "ultracite check",
    "fix": "ultracite fix",
    "fix:unsafe": "ultracite fix --unsafe"
  },
  "devDependencies": {
    "tsdown": "catalog:"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "better-auth": "catalog:",
    "zod": "catalog:",
    "drizzle-orm": "catalog:",
    "@ai-stilist/db": "workspace:*",
    "@ai-stilist/shared": "workspace:*"
  }
}
