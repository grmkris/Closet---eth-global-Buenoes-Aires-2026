DATABASE_URL="postgresql://postgres:postgres@localhost:54321/ai-stilist"
BETTER_AUTH_SECRET="generate-with-openssl-rand-base64-32"
BETTER_AUTH_URL="http://localhost:3000"
CORS_ORIGIN="http://localhost:3001"

# AI Provider API Keys
GOOGLE_GEMINI_API_KEY='api-key'

# CDP (Coinbase Developer Platform) Configuration
CDP_API_KEY_ID="your-cdp-api-key-id"
CDP_API_KEY_SECRET="your-cdp-api-key-secret"
CDP_WALLET_SECRET="your-cdp-wallet-secret"

# Default Agent Wallet (REQUIRED for x402 subscription payments)
# The wallet address that will receive subscription payments for the default "AI Stylist" agent
# This should be a valid Polygon wallet address (supports both testnet and mainnet)
# Example: DEFAULT_AGENT_WALLET_ADDRESS="0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb1"
DEFAULT_AGENT_WALLET_ADDRESS="0x0000000000000000000000000000000000000000"

# x402 Payment Integration
# Subscriptions are protected by x402 paywall middleware using auto-facilitator mode
# Supports: Polygon, Coinbase, Kamiyo, Questflow, and other x402 facilitators
# Payments are made in USDC on Polygon network (Amoy testnet in dev, mainnet in prod)
# No additional configuration needed - facilitator auto-discovery is enabled
